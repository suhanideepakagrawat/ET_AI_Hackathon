.DS_Store
**/.DS_Store
data/*.csv

# Secrets — never commit. (.env.example is the committed template.)
.env
*.env
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Node
node_modules/

# Bind's personal reference docs (plans, PDFs) — keep local, not in the shared repo
Bind's Workspace/
